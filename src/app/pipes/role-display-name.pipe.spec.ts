import { RoleDisplayNamePipe } from './role-display-name.pipe';

describe('RoleDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
